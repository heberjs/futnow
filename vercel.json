{
    "buildCommand": "cd app && npm install && ./node_modules/vite/bin/vite.js build",
    "outputDirectory": "app/dist",
    "framework": "vite",
    "rewrites": [
        {
            "source": "/api/(.*)",
            "destination": "/api/index.ts"
        }
    ]
}