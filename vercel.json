{
    "buildCommand": "cd app && npm install --force && cd ../api && npm install --force && cd ../app && npm run build",
    "outputDirectory": "app/dist",
    "framework": "vite",
    "rewrites": [
        {
            "source": "/api/(.*)",
            "destination": "/api/index.ts"
        }
    ]
}